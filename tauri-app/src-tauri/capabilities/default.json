{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window. The uv sidecar is invoked only from Rust (sidecar.rs), which does not go through this scope, so the webview gets no shell permission at all.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "opener:default",
    "updater:default",
    "process:default"
  ]
}
